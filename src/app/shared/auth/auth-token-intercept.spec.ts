import { AuthTokenIntercept } from './auth-token-intercept';

describe('AuthTokenIntercept', () => {
  it('should create an instance', () => {
    expect(new AuthTokenIntercept()).toBeTruthy();
  });
});
